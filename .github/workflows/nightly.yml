name: Nightly

on:
  push:
    branches:
    - main
  pull_request:
    types: [labeled]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build nightly release

    steps:
      - name: 'Get Previous tag'
        id: previoustag
        uses: "WyriHaximus/github-action-get-previous-tag@v1"
        if: |
          github.event_name == 'push' ||
          contains(github.event.issue.labels.*.name, 'built-as-nightly')

      - name: Build release
        uses: ./.github/workflows/build.yml
        with:
          upload_url: https://uploads.github.com/repos/UpliftGames/wally/releases/108261310/assets{?name,label}
          release_ref: ${{ steps.previoustag.outputs.tag }}-nightly